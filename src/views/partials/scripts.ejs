<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
<script src="/js/actions.js"></script>
<script>
    const carousel = document.querySelector('#galleryControl')
    const gallery =new bootstrap.Carousel(carousel, {
        wrap: false
    })

    const liELements = document.querySelectorAll('#itemListGallery li');
    console.log(liELements);

    liELements.forEach((li, index) => {
        const codigo = li.querySelector('span').childNodes[0].data;
        li.addEventListener('click', () => handleOnSelectDay(event,index));

    })


    const handleOnSelectDay = (event, target = "0") => {
        gallery.to(target);
    }

</script>
</body>
</html>
